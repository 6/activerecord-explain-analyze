require "spec_helper"

RSpec.describe Activerecord::Explain::Analyze do
  it "has a version number" do
    expect(Activerecord::Explain::Analyze::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
